*.un~
*.*~
*~
*.swp
*.hi
*.log
*.o
*.pyc
nodes
tags

/* only keep plugin manager */
tmux/plugins
!tmux/plugins/tpm

vim/autoload/plug.vim.old

/* spell files are pretty big */
vim/spell/en.utf-8.*

vim/view/

/* compiled spell files */
vim/spell/*.spl

/* don't include nvim symlink in repo; it is handled by dotbot */
vim/init.vim

/* don't include coc json config file. Configs are in vimrc for personal/work 
 * profiles separation. The json file is only for local machine configs. */
vim/coc-settings.json

vim/.netrwbook

fish/fish_history
